<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WanderLust</title>
  </head>
  <body>
    <h3>All Listings</h3>
    <form action="/api/v1/listings/new" method="GET">
      <button>Create New Listing</button>
    </form>
    <ul>
      <% for (let listing of allListings) { %>
      <li>
        <a href="/api/v1/listings/<%= listing._id %>"> <%= listing.title %> </a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
